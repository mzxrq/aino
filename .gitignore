# .gitignore for the Stock Anomaly Detection project
# Keep secrets and environment copies out of version control.

# Environment files
# - `.env.backend` contains sensitive secrets (JWT, LINE secrets, DB URIs)
# - `.env` is often a local copy of the active environment and can contain secrets
# - `.env.local` is used by many frameworks for local overrides
.env.backend
.env
.env.local
# Ignore all env files that are not examples; allow `.env.example`
.env.*
!.env.example

# Node.js
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Build outputs
dist/
build/
coverage/

# Local tests (smoke/integration) — keep out of git
tests/

# Python
__pycache__/
*.py[cod]
*.pytest_cache/
venv/
env/
.venv/

# IDEs and OS files
.vscode/
.idea/
*.sublime-workspace
*.sublime-project
.DS_Store
Thumbs.db

# Model artifacts (optional): if your models are large, keep them out of git.
# Uncomment if you don't want to track serialized model files in `resource/models/`
# resource/models/
